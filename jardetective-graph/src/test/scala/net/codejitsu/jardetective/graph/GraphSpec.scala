package net.codejitsu.jardetective.graph

class GraphSpec {
  // - define low level graph primitives (own model)
  // - all business logic belongs to the upper level (this level is kind of dao)
  // - start embedded neo4j
  // - test your primitive operations like node exists, path exists, add/delete node
}
